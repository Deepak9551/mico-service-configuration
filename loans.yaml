spring:
  application:
    name: loans
  datasource:
    url: jdbc:h2:file:./loans/data/loan
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  h2:
    console:
      enabled: true

  sql:
    init:
      mode: always
      schema: classpath:sql/schema.sql

server:
  port: 8082

build:
  version: "0.1"

cards:
  message: "Welcome to EazyBank loans related Local APIs "
  contactDetails:
    name: "Smitha Ray - QA Lead"
    email: "smitha@eazybank.com"
  onCallSupport:
    - (666) 265-3765
    - (666) 734-8371